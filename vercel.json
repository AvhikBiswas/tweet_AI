{
  "builds": [{ "src": "src/**/*.ts", "use": "@vercel/node" }],
  "routes": [
    { "src": "/api/v1/(.*)", "dest": "v1/index.ts" },
    { "src": "/api/(.*)", "dest": "routes/index.ts" }
  ]
}
